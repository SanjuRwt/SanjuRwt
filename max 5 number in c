#include<stdio.h>
int main()
{
   int n,i,max;
   for(i=1;i<=5;i++)
   {
   	printf("Enter number:");
   	scanf("%d",&n);
   	if(i==1)
   	{
   	max=n;	
	}
	if(n>max)
	{
	max=n;
	}
   }
   printf("max:%d",max);
}
