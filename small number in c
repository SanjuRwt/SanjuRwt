#include<stdio.h>
int main()
{
   int n,i,small;
   for(i=1;i<=5;i++)
   {
   	printf("Enter number:");
   	scanf("%d",&n);
   	if(i==1)
   	{
   	small=n;	
	}
	if(n<small)
	{
	small=n;
	}
   }
   printf("small:%d",small);
}
