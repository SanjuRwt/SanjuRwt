#include<stdio.h>
int main()
{
   int per;
   printf("enter persantge:");
   scanf("%d",&per);
   if(per>=60)
   {
   	printf("1st div");
   }
   else if(per>=45)
   {
   	printf("2nd div");
   }
   else if(per>=36)
   {
   	printf("3rd div");
   }else
   {
   	printf("fail");
   }
}
