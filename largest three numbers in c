#include<stdio.h>
int main()
{
   int a,b,c;
   printf("Enter three numbers:");
   scanf("%d%d%d",&a,&b,&c);
   if(a>b)
   {
   	printf("max%d",a);
   }else if(b>c)
   {
   	printf("max%d",b);
   }
   else if(c>a)
   {
   	printf("max%d",c);
   }else
   {
   	printf("max%d",a);
   }
}
