#include<stdio.h>
struct book
{
	char nm[10];
	int page;
}b[3];
int i;
void main()
{
	for(i=0;i<=2;i++)
	{
		printf("Enter book name.page number:",i+1);
		scanf("%s%d",b[i].nm,&b[i].page);
	}
	for(i=0;i<=2;i++)
	{
		printf("\n book name book pages:",i+1);
		printf("%s%d",b[i].nm,b[i].page);
	}
}
